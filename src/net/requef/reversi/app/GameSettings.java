package net.requef.reversi.app;

public class GameSettings {
    // Print possible moves and show them on the board.
    public static boolean showPossibleMoves = true;

    // Bots will have a small random delay before they make a move.
    public static boolean addDelayToBotMoves = true;
}
